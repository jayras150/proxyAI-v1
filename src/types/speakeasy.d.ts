declare module 'speakeasy' { export function generateSecret(options?: { length?: number; name?: string; issuer?: string }): { base32: string; otpauth_url?: string }; export interface Totp { verify(options: { secret: string; encoding?: string; token: string; window?: number }): boolean; } export const totp: { verify: (options: { secret: string; encoding?: string; token: string; window?: number }) => boolean; generate: (options: { secret: string; encoding?: string }) => string; }; }
